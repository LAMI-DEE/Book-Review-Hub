<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header class="auth-header">
      <h1 class="auth-title">Login</h1>
    </header>
    <main class="auth-main">
     <div class="aut-card">
      <% if (messages.error) { %>
        <div class="alert alert-danger"><%= messages.error %></div>
      <% } %>
      <form action="/login" method="POST" class="auth-form">
        <input type="email" name="username" placeholder="Email" required class="auth-input" />
        <input type="password" name="password" placeholder="Password" required class="auth-input" />
        <button type="submit" class="auth-btn">Login</button>
      </form>
      <div class="auth-google">
        <a href="/auth/google" class="auth-btn google-btn">Sign in with Google</a>
      </div>
      <p class="donot">Don't have an account? <a class="re-reg" href="/register">Register</a></p>
     </div>
    </main>
  </body>
</html>